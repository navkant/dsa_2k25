# Given a string A of size N consisting only of lowercase alphabets. The only operation allowed is to insert characters
# in the beginning of the string. Find and return how many minimum characters are needed to be inserted to make the
# string a palindrome string.


class Solution:
    # @param A : string
    # @return an integer

    def solve(self, A):
        pass


if __name__ == '__main__':
    a = 'babc'
    obj = Solution()
    ans = obj.solve(a)
    print(f'ans is {ans}')

    [2, 3, -8, 7, -1, 2, 3]
    [2, 5, -3, 4, 3, 5, 8]

